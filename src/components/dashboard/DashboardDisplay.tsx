import React, { Fragment } from 'react';

type Props = {
    token: string;
};

export const DashboardDisplay = (props: Props) => {
    return (
        <Fragment>
              <h1>Dashboard</h1>
        </Fragment>
    );
};
