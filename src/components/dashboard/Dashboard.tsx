import React, { Fragment } from 'react'

type Props = {
    token: string;
}

export const Dashboard = (props: Props) => {
    return (
        <Fragment>
            <h1>Dashboard</h1>
        </Fragment>
    )
}
