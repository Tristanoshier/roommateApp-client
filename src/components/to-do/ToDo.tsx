import React, { Fragment } from 'react';

type Props = {
    token: string;
};

export const ToDo = (props: Props) => {
    return (
        <Fragment>
            <h1>To Do</h1>
        </Fragment>
    );
};
