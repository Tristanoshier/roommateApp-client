import React, { Fragment } from 'react';

type Props = {
    token: string;
}

export const AddStoreItemDisplay = (props: Props) => {
    return (
        <Fragment>
            
        </Fragment>
    );
};
