import React, { Fragment } from 'react';

type Props = {
    token: string;
};

export const ToBuy = (props: Props) => {
    return (
        <Fragment>
            <h1>To Buy</h1>
        </Fragment>
    );
};
