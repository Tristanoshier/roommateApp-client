import React, { Fragment } from 'react'

type Props = {
    token: string;
}

export const People = (props: Props) => {
    return (
        <Fragment>
            <h1>People</h1>
        </Fragment>
    )
}
